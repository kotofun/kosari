import DisplayCharacter from './DisplayCharacter'

// TODO: create Satan sprite

export default class extends DisplayCharacter {
  constructor (ctx, x, y) {
    super(ctx, x, y, 'character.satan')
  }
}
